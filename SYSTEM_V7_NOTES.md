# 龙空龙复盘系统 V7

## 本次重构目标

V7 不再把复盘系统做成指标堆叠或主观评分器，而是做成一个从环境到风险的交易状态机：

1. 环境决定是否交易。
2. 龙头身份决定交易谁。
3. 四大战法决定如何进入和退出。
4. 证伪条件决定何时结束交易。
5. R 值决定仓位和账户回撤上限。

## 四大战法

- `RA-1` 绕严重异动：T-1 买健康分歧，T 日在动态红线前兑现。
- `RA-2` 博弈出监管：最后监管日拿筹码，解除日卖第一次加速；盘后检查续杯。
- `LM-1` 龙回头：缩量止跌后首次主动转强；反抽与二波必须分开管理。
- `CS-1` N 字 / U 型：只做右侧确认，形态必须叠加辨识度、题材或催化。

## 新的数据纪律

- 一笔交易只有一个原始理由。
- 原始理由失效后不得切换叙事继续持仓。
- `1R` 默认等于账户净值的 `0.5%`，可在页面修改。
- 默认单日亏损达到 `-2R` 停止新增交易。
- 连续三笔模式失败后降仓，不通过扩大风险翻本。
- 程序仍然公开部署；个人复盘记录仍只写入独立私有数据仓库。

## 兼容性

数据结构升级到 `schemaVersion: 7`。旧版 V5/V6 记录仍可读取；新字段在旧记录中为空，不改写历史内容。
